# Model files

Place or link the required assets as follows:

```text
models/dinov3_repo/hubconf.py
models/dino/dinov3_vits16_pretrain_lvd1689m-08c60483.pth
models/sg_rife/flownet.pkl
```

Alternatively set `SG_RIFE_DINOV3_REPO`, `SG_RIFE_DINO_CHECKPOINT`, and `SG_RIFE_CHECKPOINT` before starting ComfyUI.
